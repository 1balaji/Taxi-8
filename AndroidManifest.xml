<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ua.dp.kulish.taxi"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="15" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".TaxiService"
            android:label="@string/title_activity_taxi_service" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Favorits"
            android:label="@string/text_favorits" >
           
        </activity>
        <activity
            android:name=".City"
            android:label="@string/text_city" >
        
        </activity>
        <activity
            android:name=".Order"
            android:label="@string/text_order" >
        
        </activity>
        <activity
            android:name=".Info"
            android:label="@string/text_info" >
        
        </activity>
    </application>

</manifest>